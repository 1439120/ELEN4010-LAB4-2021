/* eslint-env jest */
// let classList = require("./../classList")
describe('Jest template', () => {
  test('Sum of two numbera', () => {
    const a = 1
    const b = 2
    const sum = a + b
    expect(sum).toBe(a + b)
  })
})
